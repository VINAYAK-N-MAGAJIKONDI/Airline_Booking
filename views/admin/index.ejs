<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #1e293b; /* Dark blue background */
            color: #f8f9fa; /* Light text color */
        }
        .dashboard-card {
            background-color: #28354a; /* Darker blue card background */
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
            padding: 20px;
            margin-bottom: 20px;
            transition: transform 0.3s ease-in-out; /* Add transition for hover effect */
        }
        .dashboard-card:hover {
            transform: scale(1.05); /* Scale up on hover */
        }
        .dashboard-icon {
            font-size: 3em;
            color: #ffa500; /* Orange icon color */
            margin-bottom: 15px;
            transition: transform 0.5s ease-in-out; /* Add transition for icon */
        }
        .dashboard-card:hover .dashboard-icon {
            transform: rotate(360deg); /* Rotate icon on hover */
        }

        .card-title {
            font-weight: bold;
            color: #f8f9fa;
        }
        a {
            text-decoration: none;
            color: #f8f9fa; 
        }
        a:hover {
            color: #ffa500;  /* Orange hover color for links */
        }
        .display-4 {
            color: #ffa500;  /* Orange for the main heading */
        }

    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="display-4 text-center mb-4">Welcome to Admin Dashboard</h1>

        <div class="row">
            <div class="col-md-4">
                <div class="dashboard-card text-center">
                    <a href="/admin/airports" class="text-decoration-none text-dark">
                        <i class="bi bi-airplane-engines-fill dashboard-icon"></i>
                        <h5 class="card-title">Manage Airports</h5>
                    </a>
                </div>
            </div>
            <div class="col-md-4">
                <div class="dashboard-card text-center">
                    <a href="/admin/flights" class="text-decoration-none text-dark">
                        <i class="bi bi-airplane dashboard-icon"></i>
                        <h5 class="card-title">Manage Flights</h5>
                    </a>
                </div>
            </div>
            <div class="col-md-4">
                <div class="dashboard-card text-center">
                    <a href="/admin/bookings" class="text-decoration-none text-dark">
                        <i class="bi bi-calendar2-check dashboard-icon"></i>
                        <h5 class="card-title">View Bookings</h5>
                    </a>
                </div>
            </div>
        </div>

            <!-- ... (Rest of your card HTML as before) ... -->
        </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>